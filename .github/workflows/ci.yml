name: CI

on:
  push:
  pull_request:

jobs:
  contract-and-bridge-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate plugin descriptor
        run: test -f Nuxie.uplugin

      - name: Run Journey contract checks
        run: node ./scripts/test-contract.mjs

      - name: Verify the compiled Android bridge
        run: ./scripts/test-android-bridge.sh
