# nuxie-ue

Native-first Unreal Engine plugin for Nuxie mobile SDKs.

This repository hosts the plugin distributed as a submodule in the Nuxie monorepo at `packages/nuxie-ue`.

## Status

Bootstrapped scaffold. Runtime APIs, bridges, tests, and docs are implemented incrementally via subsequent commits.
